Atribut State:
activeTool: menyimpan string alat aktif ('select', 'add-process', 'add-worker', dsb).

Aksi (Functions):
setActiveTool(tool): mengubah mode alat saat user mengeklik ikon di Toolbar.
